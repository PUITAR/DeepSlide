You are generating ONLY the CSS/theme for a web-based slide deck.

Goal: keep the style "Keynote launch event" (dark navy gradient, glass cards, subtle neon accents).
IMPORTANT: DO NOT output a full HTML document. To avoid truncation, output ONLY:

1) An optional single line: <title>Deck Title</title>
2) A single <style> ... </style> block that defines ALL necessary CSS.
3) A DESIGN SPEC comment block:

<!-- DESIGN_SPEC_BEGIN
(short, <= 25 lines)
DESIGN_SPEC_END -->

Hard constraints:
- Output must be plain text, no Markdown fences.
- No <script>, no <body>, no external resources.
- CSS should be self-contained. Prefer system fonts. No remote fonts.
- The runtime will inject slides into: .stage > section.slide
  (only one visible has class .is-active)
- Controls exist with ids: #btnPrev #btnNext #curSlide #totalSlides #progressBar
- Slides may contain these component classes:
  hero, subtitle, chips, chip, card, card-title, bullets, media-card, media-img, media-meta,
  callout-row, callout-pill, callout-line, divider, math, kpi, stat, grid-2col, grid-3col,
  runway, step, watermark.

UI-FIX constraints (must be reflected in CSS):
- callout-pill MUST be fit-content (no giant boxes for short text), with max-width cap (~40%).
- bullets MUST have ONLY ONE marker (arrow). list-style: none; no extra dots.
- divider MUST be SINGLE line; do not style <hr>. Provide .divider only.
- .math should look like an inline formula: mono font + subtle background + border.
- .kpi should pop (weight + glow) but only applies when span.kpi exists.
- Typography scale: avoid too small text; set sensible clamp() defaults.

Output guidelines:
- Keep CSS moderate length (avoid huge frameworks). Prefer CSS variables for colors.
- Provide 2xl rounded corners and soft shadows for glass cards.
- Ensure the deck is responsive; use max-width containers and clamp font sizes.
- Add subtle transitions for slide change (opacity/transform).

DESIGN_SPEC must include:
- design_language, base_bg, text_color, accent colors
- bullet marker choice
- how to render callout pills & dividers
- how to treat math and KPI emphasis
