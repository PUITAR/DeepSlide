# Qwen3.5 (public-materials) LaTeX package

## Build

```bash
cd qwen35_paper
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
```

Figures are under `figures/`.

## Notes

- Videos referenced in the model card are intentionally omitted (per request).
- Some screenshots are sourced from public reporting that reproduces the official release figures.
