Today, I'll introduce the landscape of LLM post-training. Currently, there are two primary paradigms that dominate this field: Supervised Fine-Tuning, or SFT, and Reinforcement Learning, or RL.

SFT involves training models on curated input-output pairs, effectively aligning them with human demonstrations through static datasets. On the other hand, RL refines model behavior in ways that are difficult to capture with SFT alone.

A particularly popular framework within RL is Reinforcement Learning from Human Feedback, or RLHF, which optimizes LLMs against reward models trained on human preferences. Algorithms like Proximal Policy Optimization, or PPO, and Group Relative Policy Optimization, or GRPO, have become cornerstones in this domain.
<next>Despite their widespread use, there's a significant challenge in the current landscape. SFT and RL are often treated as completely separate paradigms, leading to a fragmented understanding of post-training.

We've seen an explosion of specialized algorithms - PPO, GRPO, CISPO, GSPO, and many others - each with their own implementations and theoretical justifications. However, there's limited theoretical connection between these approaches.

This fragmentation creates practical challenges: it's difficult to choose between different approaches or combine them effectively. There's a clear research gap in systematically analyzing what makes each approach work and how they relate to each other.
<next>To address these challenges, we propose a unified framework for understanding LLM post-training. Our key contribution is adopting a unified perspective that views both SFT and RL as post-training objectives.

We show that all these algorithms can be expressed in a common gradient form: the Unified Policy Gradient Estimator. This estimator consists of four key components that capture the differences between algorithms.

First, the reference policy determines what we're comparing against. Second, the advantage estimate captures the quality of generated responses. Third, a stability indicator ensures training stability. And fourth, the gradient of the policy itself.

This unified framework enables systematic analysis and comparison of different algorithms, helping us understand their relationships and trade-offs.
<next>Let me summarize what we've covered. We started with the current landscape of LLM post-training, dominated by SFT and RL approaches. We identified the need for a unified view due to fragmented understanding and algorithm proliferation.

Our solution is a unified framework that expresses all algorithms in a common gradient form with four key components. This provides a systematic way to analyze and compare different post-training approaches.

The key insight is that despite their apparent differences, all these algorithms share the same underlying structure. In the following sections, we'll explore the theoretical derivation of this unified framework and its practical implications for algorithm design and implementation.
<next>Let me introduce our Unified Policy Gradient Estimator. We start from a simple but powerful insight: all post-training algorithms, whether SFT or RL, share a common objective. They all aim to maximize expected reward by increasing the likelihood of positive trajectories while decreasing the likelihood of negative ones. This means SFT and RL are not fundamentally different - they're optimizing the same thing!
<next>Our unified framework breaks down the gradient into four essential components. First, we have a stabilization mask that implements trust-region constraints like PPO clipping. Second, there's the reference policy that determines how we sample trajectories. Third, we have a unified advantage estimate that combines both RL and SFT advantages. And finally, the likelihood gradient term. Mathematically, this gives us a clean, modular structure where each component plays a specific role.
<next>The heart of our framework is the unified advantage function. It combines two terms: the RL advantage, which is simply the raw reward, and the SFT advantage, which measures data adherence. The SFT term has a coefficient mu that controls its strength. What's beautiful here is that both terms can coexist in the same estimator. The choice of reference policy determines whether we're doing on-policy updates with the old policy or offline updates with the behavior policy.
<next>Putting it all together, we get this elegant gradient expression. In practice, we multiply by a trust-region mask for stability. RL methods often use structured advantages like group normalization to reduce variance. The key insight is that the gradient decomposes into two terms: a reward term sampled from the current policy, and a data-adherence term sampled from the behavior policy. Both terms map to the same unified estimator, just with different choices of reference policy.
<next>So what does this unified framework give us? First, it provides a unified view showing that SFT and RL are optimizing the same objective. Second, it offers a modular design where each component has a clear role. Third, it enables joint training of SFT and RL without conflict. The framework is flexible enough to accommodate on-policy RL, offline RL, and supervised fine-tuning. Most importantly, it gives us a theoretical foundation derived from simple reward maximization, enabling practical hybrid training approaches. In essence, we've created a single framework that unifies seemingly different methods.
