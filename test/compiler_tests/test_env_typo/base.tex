%!TeX encoding = UTF-8
%!TeX program = xelatex

\documentclass[10pt]{beamer}
\usetheme{Feather}

%-------------------------------------------------------
% INCLUDE PACKAGES
%-------------------------------------------------------

\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage[T1]{fontenc}
\usepackage{times}
\usepackage{appendixnumberbeamer} % For appendix slide numbering

%-------------------------------------------------------
% DEFFINING AND REDEFINING COMMANDS
%-------------------------------------------------------

% colored hyperlinks
\newcommand{\chref}[2]{
  \href{#1}{{\usebeamercolor[bg]{Feather}#2}}
}

% Remove navigation symbols
\setbeamertemplate{navigation symbols}{}

% Simplify footline
\setbeamertemplate{footline}{
  \hfill\usebeamercolor[fg]{page number in head/foot}%
  \usebeamerfont{page number in head/foot}%
  \insertframenumber\,/\,\inserttotalframenumber%
  \hspace{2em}\vskip2pt%
}

% Reduce space before and after itemize/enumerate
\setlength{\leftmargini}{1em}
\setlength{\leftmarginii}{1em}

% Compact itemize
\setbeamertemplate{itemize items}[circle]
\setbeamertemplate{itemize subitem}[circle]
\setbeamertemplate{itemize subsubitem}[circle]

% Compact enumerate
\setbeamertemplate{enumerate items}[default]

% Reduce space in lists
\addtobeamertemplate{itemize item}{}{\vspace{-0.3\baselineskip}}
\addtobeamertemplate{itemize subitem}{}{\vspace{-0.3\baselineskip}}
\addtobeamertemplate{itemize subsubitem}{}{\vspace{-0.3\baselineskip}}

% Reduce vertical spacing in frames
\setlength{\parskip}{0.5ex}

\input{title}

\begin{document}

\begin{frame}[noframenumbering] 
  \titlepage 
\end{frame}

% Optional: Remove content slide if not needed
%\begin{frame}{Content}{}
%\tableofcontents
%\end{frame}

\input{content}

% references
\begin{frame}
    \frametitle{References}
    \bibliographystyle{plain}
    \bibliography{ref}
\end{frame}

\end{document}