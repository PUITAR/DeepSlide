# Assets

该目录用于存放 README 等文档展示素材（截图、图表、功能图标等）。

默认素材来源于 `DeepSlide-Arxiv/`（论文工程目录）。首次拉取仓库后可执行：

```bash
bash assets/sync_from_paper.sh
```

同步内容：

- `assets/paper/`：论文图表与界面截图（jpg/png；pdf 会自动转 png 并移除）
- `assets/icons/`：论文中使用的能力图标与提供商图标（png）
